DATABASE_URL="postgresql://aiorbit:aiorbit@localhost:5433/mock_aiorbit?schema=public"
# Isolated from backend (5432/aiorbit). Mock DB lives in mock_pgdata volume.
# PGDATA layout: cluster -> mock_aiorbit subdir -> DirectoryItem heap files (8KB pages) + WAL + shared_buffers cache
